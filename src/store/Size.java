package store;

// Enum to represent the different sizes
public enum Size {
	XS,
    S,
    M,
    L,
    XL,
    XXL,
    ONE_SIZE, // For items like caps or scarfs where one size fits all
    ADJUSTABLE; // For adjustable items
}
